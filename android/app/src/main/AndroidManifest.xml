<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Voice notes recording (bidirectional chat messaging) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Camera: proof-of-work / service documents, work gallery, chat photos,
         profile pictures (image_picker -> ImageSource.camera). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Gallery / media (image_picker -> ImageSource.gallery).
         Android 13+ : granular media permissions. The system photo picker
         itself needs no runtime grant on 33+, but declaring these keeps the
         picked paths directly readable on devices that fall back to the
         legacy picker flow. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Android 14+ : user-granted PARTIAL photo access (only the media the
         user explicitly selects). Pair with READ_MEDIA_IMAGES. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED"/>
    <!-- Android 12 and below : legacy gallery read access. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- GPS is an enhancement, not a hard requirement: devices without a
         GPS chip can still install and type their address manually. -->
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false"/>
    <!-- Camera + autofocus are OPTIONAL hardware (CodeRabbit / Play Store):
         the app only uses the camera opportunistically (image_picker) and
         gallery picking is always available — declaring required="false"
         keeps devices without a built-in camera installable on Google Play. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>
    <application
        android:label="allo_service_pro"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
